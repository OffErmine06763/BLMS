TODO:
- understand the residual plots for logistic regression (help me pls)
- use polynomial/interaction/transformed features? do clustering for "online" related features
- variable selection including those transformed features?
- try new betaprior (distribution from which are sampled the feature coefficients given the data, the default is CCH(alpha = 0.5, beta = n, s = 0)) and modelprior (probability distribution of the number of predictors each model will use, default is uniform) for bas.glm